/** Public repository for the app, SDK and community plugins. */
export const SOURCE_URL = "https://github.com/ghota-tech-solutions-sass/flexwall";

/** The app's origin in local development and tests, when NEXT_PUBLIC_APP_URL isn't set. */
export const LOCAL_APP_URL = "http://localhost:3000";
