/**
 * Where the source code is published. Null while the repository is private:
 * pages then don't link to it or tell visitors to read it. Set it the day the
 * repository goes public.
 */
export const SOURCE_URL: string | null = null;

/** The app's origin in local development and tests, when NEXT_PUBLIC_APP_URL isn't set. */
export const LOCAL_APP_URL = "http://localhost:3000";
