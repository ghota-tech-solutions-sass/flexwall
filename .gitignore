node_modules/
.next/
*.tsbuildinfo
.env
.env*.local
# Provider keys and certificates kept for local testing. Tracked here rather
# than in .git/info/exclude, which stays on one machine and protects no clone.
.secrets/
.claude/
*.log
.DS_Store
.tmp/
.tmp-*
# written by `next dev`, see apps/web/README.md
AGENTS.md
CLAUDE.md

# terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
tfplan
