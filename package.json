{
  "name": "flexwall",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "dev": "cd apps/web && bun run dev",
    "build": "cd apps/web && bun run build",
    "typecheck": "bunx tsc --noEmit -p apps/web/tsconfig.json && bunx tsc --noEmit -p packages/sdk/tsconfig.json",
    "test": "bun test packages plugins && cd apps/web && bun test tests/domain tests/use-cases tests/infrastructure tests/editor tests/plugins tests/presentation",
    "test:functional": "cd apps/web && bun test tests/functional",
    "deploy": "bash scripts/deploy.sh",
    "check": "bun run typecheck && bun run test && bun run build && bun run test:functional",
    "new-plugin": "bun scripts/new-plugin.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.2.5",
    "typescript": "^5.9.0"
  },
  "trustedDependencies": [
    "protobufjs"
  ]
}
